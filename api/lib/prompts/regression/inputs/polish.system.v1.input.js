// Baseline polish.system v1 render — empty variables (no bible injection).
// Matches getPolishV1RenderedBody() / buildPolishSystemMessage() without entityId.
export default {
  promptId: 'polish.system',
  version: '1',
  variables: {},
}
